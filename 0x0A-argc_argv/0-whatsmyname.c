#include <stdio.h>

/**
 * main - entry point
 * @argc: input integer
 * @argv: input character
 * Return: Always 0
 */


int main(int argc attribute ((unused)) char *argv[])
{

	printf("%s\n", *argv);
	return (0);
}
